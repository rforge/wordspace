import(Matrix)

useDynLib(wordspace, .registration=TRUE)
export(R_sqrt)


export(read.dsm, check.dsm)
export(dsm.score)
export(rowNorms, normalize.rows)
export(dsm.projection)
export(dist.matrix, nearest.neighbours)
export(match.split)
export(context.vectors)

S3method(subset, dsm)

S3method(rbind, dsm)
S3method(cbind, dsm)

S3method(merge, dsm)
