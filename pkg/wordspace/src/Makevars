# -- enable OpenMP parallelisation if available on this platform
PKG_CFLAGS = $(SHLIB_OPENMP_CFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CFLAGS)

# -- force OpenMP on Mac OS X (supported by compiler, but not used by default toolchain)
# PKG_CFLAGS = -fopenmp
# PKG_LIBS = -fopenmp
